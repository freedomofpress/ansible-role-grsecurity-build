---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: grsecurity-build-stable3-stretch
    image: debian
    # hash is debian stretch
    image_hash: sha256:75f7d0590b45561bfa443abad0b3e0f86e2811b1fc176f786cd30eb078d1846f
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: workstation
  test_sequence:
    # Far too many linting violations, punting on cleanup.
    # - lint
    - destroy
    - dependency
    - syntax
    - create
    - converge
    # No tests written yet, punting for now.
    # - verify
    - destroy
verifier:
  name: testinfra
  lint:
    name: flake8
